// interface enforces a class go implement parseInput methods designed to ask user for an input and makes sure input is correct
public interface UserInput{
    int parseInput(String s);
    int parseInput(String s, int range);
}
    